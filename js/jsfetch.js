const username = "ZainabShujat"; // replace with your GitHub username

fetch(`https://api.github.com/users/${ZainabShujat}`)
  .then(response => response.json())
  .then(data => {
    console.log("GitHub Info:", data);
    // You can access fields like data.name, data.public_repos, etc.
  })
  .catch(error => {
    console.error("Error fetching GitHub info:", error);
  });
const username = "ZainabShujat"; // replace with your GitHub username

fetch(`https://api.github.com/users/${ZainabShujat}`)
  .then(response => response.json())
  .then(data => {
    console.log("GitHub Info:", data);
    // You can access fields like data.name, data.public_repos, etc.
  })
  .catch(error => {
    console.error("Error fetching GitHub info:", error);
  });
const username = "ZainabShujat"; // replace with your GitHub username

fetch(`https://api.github.com/users/${ZainabShujat}`)
  .then(response => response.json())
  .then(data => {
    console.log("GitHub Info:", data);
    // You can access fields like data.name, data.public_repos, etc.
  })
  .catch(error => {
    console.error("Error fetching GitHub info:", error);
  });